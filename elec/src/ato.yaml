ato-version: ^0.0.20
paths:
  build: ../../build/
builds:
  default:
    root-file: spin_servo_nema17.ato
    root-node: spin_servo_nema17.ato:SpinServoNEMA17
    targets:
      - designators
      - netlist-kicad6
      - bom-jlcpcb
    kicad-project-dir: ../layouts/default/
